{
  "name": "relocate-torrents",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "run": "cd dist && node bundle.js",
    "build": "webpack",
    "build:dev": "webpack --watch",
    "test": "jest",
    "test:watch": "jest --watchAll"
  },
  "dependencies": {
    "bencode-js": "0.0.8",
    "readline-sync": "^1.4.7"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.2",
    "babel-preset-es2015": "^6.24.1",
    "jest": "^22.0.4"
  }
}
